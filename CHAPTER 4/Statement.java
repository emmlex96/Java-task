There Similarities: Both evaluate a boolean condition before deciding whether   to execute a body of code.

Differences: The if statement checks the condition once and executes the body at most one time (or zero times if false).

 The while statement keeps re-evaluating the condition after each execution of the body, repeating until the condition becomes false.
